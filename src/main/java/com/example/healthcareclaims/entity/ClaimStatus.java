package com.example.healthcareclaims.entity;

// Enum means a variable can have only a fixed set of values.
// This keeps claim status limited to valid choices.
public enum ClaimStatus {
    PENDING,
    APPROVED,
    REJECTED
}
